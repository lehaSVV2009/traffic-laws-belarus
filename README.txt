Simple plain JavaScript application for traffic rules tests in Belarus.

Package manager like npm is not used.

There are 2 index.html files. The first one (index-dev.html) uses non-minimized js files. The second one uses minimized app.min.js (index.html).
Use https://jscompress.com for JavaScript files compression.

All the questions and traffic rules chapters are stored in json files.